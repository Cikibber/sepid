# manifests/ — metadata only. No audio, no consent forms (see §9.6).
# Frozen manifests are immutable and versioned; full schema: src/sepid/data/schema.py.
# Minimal unified loader columns: mixture_path, source_1_path, source_2_path, duration.
